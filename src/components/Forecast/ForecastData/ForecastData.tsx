// import { PredictiHttpResult } from "../../../api/api.types";

// interface ForecastDataProps {
//   requestedData: PredictiHttpResult;
// }

// const ForecastData = ({ requestedData }: ForecastDataProps) => {
//   const keyList = Object.keys(requestedData.ext_data);
//   return (
//     <div>
//       {keyList.map((key) => (
//         <div>{requestedData.ext_data[key]}</div>
//       ))}
//     </div>
//   );
// };

// export default ForecastData;
